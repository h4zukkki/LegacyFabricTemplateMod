pluginManagement {
    repositories {
        maven(uri("https://maven.fabricmc.net/"))
        maven(uri("https://maven.legacyfabric.net/"))
        mavenCentral()
        gradlePluginPortal()
    }
}